import Link from 'next/link';import type {Product} from '@/lib/types';import {money} from '@/lib/validation';
export function BookGrid({books}:{books:Product[]}){return books.length?<div className="cards">{books.map(b=><article className="card" key={b.id}><Link href={'/libro/'+b.slug}>{b.cover?<img src={b.cover} alt={'Portada de '+b.title} loading="lazy" width="260" height="340"/>:<div className="notice">Portada pendiente</div>}<p className="eyebrow">{b.publisher}</p><h2>{b.title}</h2><p>{b.author}</p><p>{money(b.price_cents)} <small>MXN</small></p><small>{b.stock>0?'Disponible':'Agotado'}</small></Link></article>)}</div>:<div className="empty-editorial"><p>No hay títulos para mostrar.</p><small>El catálogo aparecerá aquí cuando la librería confirme sus libros y disponibilidad.</small></div>}
