import {PageShell} from '@/components/site-shell';import {all,one} from '@/lib/db';import {BookGrid} from '@/components/book-grid';import type {Product} from '@/lib/types';import {notFound} from 'next/navigation';
export default async function Page({params}:{params:Promise<{slug:string}>}){const a=await one<{name:string;description:string}>('SELECT name,description FROM categories WHERE slug=?',(await params).slug);if(!a)notFound();return <PageShell><p className="eyebrow">CATEGORÍAS</p><h1>{a.name}</h1><p className="prose">{a.description}</p><BookGrid books={await all<Product>('SELECT * FROM products WHERE category=? AND published=1',a.name)}/></PageShell>}
