import {PageShell} from '@/components/site-shell';import {Admin} from '@/components/admin';import {notFound} from 'next/navigation';export const metadata={title:'Administración · El Diván',robots:{index:false,follow:false}};export default async function Page({params}:{params:Promise<{section:string}>}){const {section}=await params;if(!['productos','autores','categorias','pedidos','envios','facturas','blog','suscriptores','vacaciones','integraciones','actividad'].includes(section))notFound();return <PageShell><Admin section={section}/></PageShell>}
