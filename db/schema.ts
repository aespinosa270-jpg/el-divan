import { sqliteTable, text, integer, real, uniqueIndex, check } from 'drizzle-orm/sqlite-core';
import {sql} from 'drizzle-orm';
export const products = sqliteTable('products', {id:text('id').primaryKey(),slug:text('slug').notNull().unique(),title:text('title').notNull(),author:text('author').notNull(),publisher:text('publisher').notNull(),description:text('description').notNull(),isbn:text('isbn'),sku:text('sku').unique(),priceCents:integer('price_cents').notNull(),stock:integer('stock').notNull(),cover:text('cover'),images:text('images').notNull().default('[]'),category:text('category').notNull(),tags:text('tags').notNull().default(''),weight:real('weight').notNull(),length:real('length').notNull(),width:real('width').notNull(),height:real('height').notNull(),published:integer('published').notNull().default(0),featured:integer('featured').notNull().default(0),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()});
export const authors = sqliteTable('authors',{id:text('id').primaryKey(),name:text('name').notNull().unique(),slug:text('slug').notNull().unique(),bio:text('bio').notNull().default('')});
export const categories = sqliteTable('categories',{id:text('id').primaryKey(),name:text('name').notNull().unique(),slug:text('slug').notNull().unique(),description:text('description').notNull().default('')});
export const posts = sqliteTable('posts',{id:text('id').primaryKey(),slug:text('slug').notNull().unique(),title:text('title').notNull(),excerpt:text('excerpt').notNull(),body:text('body').notNull(),author:text('author').notNull(),category:text('category').notNull(),cover:text('cover'),published:integer('published').notNull().default(0),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()});
export const settings = sqliteTable('settings',{id:text('id').primaryKey(),value:text('value').notNull()});
export const sessions = sqliteTable('sessions',{hash:text('hash').primaryKey(),role:text('role').notNull(),expiresAt:integer('expires_at').notNull()});
export const rateLimits = sqliteTable('rate_limits',{key:text('key').primaryKey(),count:integer('count').notNull(),expiresAt:integer('expires_at').notNull()});
export const audit = sqliteTable('audit',{id:text('id').primaryKey(),actor:text('actor').notNull(),action:text('action').notNull(),entity:text('entity').notNull(),createdAt:text('created_at').notNull()});
export const carts = sqliteTable('carts',{id:text('id').primaryKey(),createdAt:text('created_at').notNull()});
export const cartItems = sqliteTable('cart_items',{id:text('id').primaryKey(),cartId:text('cart_id').notNull().references(()=>carts.id),productId:text('product_id').notNull().references(()=>products.id),quantity:integer('quantity').notNull()},t=>[uniqueIndex('cart_product').on(t.cartId,t.productId)]);
export const orders = sqliteTable('orders',{id:text('id').primaryKey(),cartId:text('cart_id').notNull(),requestKey:text('request_key').unique(),reserved:integer('reserved').notNull().default(0),status:text('status').notNull(),email:text('email').notNull(),address:text('address').notNull(),subtotal:integer('subtotal').notNull(),shipping:integer('shipping').notNull(),total:integer('total').notNull(),paymentId:text('payment_id').unique(),paymentForm:text('payment_form'),createdAt:text('created_at').notNull()});
export const orderItems = sqliteTable('order_items',{id:text('id').primaryKey(),orderId:text('order_id').notNull().references(()=>orders.id),productId:text('product_id').notNull(),title:text('title').notNull(),quantity:integer('quantity').notNull(),priceCents:integer('price_cents').notNull()});
export const externalEvents = sqliteTable('external_events',{key:text('key').primaryKey(),provider:text('provider').notNull(),status:text('status').notNull(),createdAt:text('created_at').notNull()});
export const shipments = sqliteTable('shipments',{id:text('id').primaryKey(),orderId:text('order_id').notNull().unique().references(()=>orders.id),status:text('status').notNull(),providerId:text('provider_id').unique(),tracking:text('tracking'),cost:integer('cost'),error:text('error')});
export const invoices = sqliteTable('invoices',{id:text('id').primaryKey(),orderId:text('order_id').notNull().unique().references(()=>orders.id),status:text('status').notNull(),providerId:text('provider_id').unique(),fiscalData:text('fiscal_data').notNull(),payload:text('payload'),attempts:integer('attempts').notNull().default(0),nextAttemptAt:integer('next_attempt_at').notNull().default(0),xml:text('xml'),pdf:text('pdf'),error:text('error')});
export const subscribers = sqliteTable('subscribers',{id:text('id').primaryKey(),email:text('email').notNull().unique(),status:text('status').notNull(),tokenHash:text('token_hash').notNull().unique(),consentVersion:text('consent_version').notNull(),confirmExpiresAt:integer('confirm_expires_at').notNull().default(0),emailPayload:text('email_payload'),emailStatus:text('email_status').notNull().default('pending'),createdAt:text('created_at').notNull()});

export const guards=sqliteTable('operation_guards',{id:text('id').primaryKey(),valid:integer('valid').notNull()},t=>[check('operation_must_be_valid',sql`${t.valid} = 1`)]);
